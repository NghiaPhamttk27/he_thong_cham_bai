#include <iostream>
using namespace std;

int main() {
    int n;
    cin >> n;
    
    // Vòng lặp vô hạn không có điều kiện dừng và không cout
    while (true) {
        // Chạy hoài không dừng
    }

    return 0;
}